SELECT * FROM nj_teachers_salaries.teacher;
-- SELECT last_name, first_name, school, experience_total FROM nj_teachers_salaries.teacher order by experience_total desc;
-- SELECT MIN(experience_total) FROM nj_teachers_salaries.teacher;
-- SELECT COUNT(school) from nj_teachers_salaries.teacher;
-- SELECT COUNT(distinct school) from nj_teachers_salaries.teacher;
-- SELECT SUM(experience_total) FROM nj_teachers_salaries.teacher;
-- SELECT SUM(experience_total) FROM nj_teachers_salaries.teacher WHERE primary_job = "Art";
